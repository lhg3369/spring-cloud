spring:
  cloud:
    config:
      name: application
      profile: application
      label: master
      discovery:
        enabled: true
        service-id: springcloud-config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/